# Normalize line endings for distribution. Plugin files are read by Claude Code on all
# platforms — keep them LF in the repo regardless of the committer's OS.
* text=auto eol=lf

# Scripts must stay LF (node on POSIX rejects CRLF shebangs).
*.mjs text eol=lf
*.js  text eol=lf
*.cjs text eol=lf
