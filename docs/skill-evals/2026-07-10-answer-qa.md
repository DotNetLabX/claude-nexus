# Skill Evaluation — answer-qa

**Evaluator:** developer (adhoc-AnalystExtension, Step 4) · **Date:** 2026-07-10
**Scope:** `plugins/nexus-analytics/skills/answer-qa/SKILL.md` (freshly authored this round).
**Run artifacts consulted:** none — never executed; this judges the spec, not observed job
fitness, disclosed rather than concealed.

## Layer 0 — Mechanical (scripted)

```
$ node .../improve-skills/scripts/skill-lint.mjs plugins/nexus-analytics/skills/answer-qa
OK    answer-qa
```
PASS.

## Findings

### F1: Missing scope fence
**Severity:** Medium
**Layer:** 1.5
**Claim vs reality:** No "what this skill does NOT do" section — a reader can't tell this is the
LAST check (after the query has already run), not a substitute for `semantic-model-query`'s
pre-query check, and doesn't itself build or execute anything.
**Fix:** Add a scope-fence section: doesn't build/execute the query (semantic-model-query's job),
doesn't investigate unclear columns (data-investigation's job), doesn't replace the pre-query
check (it's a post-hoc gate on the response, per the existing "Check order" section). Name
`data-analyst` as the consumer. **Applied in this pass.**

### F2: No lessons-capture instruction
**Severity:** Low
**Layer:** 4.1
**Claim vs reality:** Same gap as the other two new skills.
**Fix:** Add the same one-line pointer. **Applied in this pass.**

## Checked clean

- Layer 0 (all 5 mechanical checks).
- Layer 1.1 (frontmatter promise = body — the 5-item contract (grain, filters, date range,
  constructs, caveats) and the "malformed" verdict are both implemented exactly as described).
- Layer 1.2 (no fabricated guardrail claims — this is a checklist skill, not a write/side-effect
  skill).
- Layer 1.3 (no external-system claims).
- Layer 1.4 (citation audit — `docs/semantic-model/profile.md` cited consistently as the source of
  the obligation list, never contradicted).
- Layer 1.6 (the entire skill IS a failure-mode encoding — "malformed" names the failure class
  directly and states what makes an answer fail, including the inverted-claim case: "stating a
  filter was applied when it was not").
- Layer 2.1 (one concept once — doesn't restate semantic-model-query's obligation list, only
  checks that the ANSWER named it).
- Layer 2.2 (the 5-item contract is a numbered, checkable list — closer to a mechanical self-check
  shape than the other two skills' prose, though still model-executed rather than scripted; same
  "no run evidence yet" reasoning as the other two skills applies to why it isn't converted to an
  external script this pass).
- Layer 2.3 (right weight — light skill, single file).
- Layer 2.4 (followable cold — the contract, the malformed-answer rule, and the check-order
  section are unambiguous and don't require the author's context).
- Layer 2.5 (anti-patterns — none apply: no hardcoded inventories, no fictional infrastructure, no
  multi-phase machinery to strand).
- Layer 3 overlays — none apply (not a writer, not a fan-out, not iterating an unbounded list, not
  multi-phase).
- Layer 4.2/4.3 — N/A pre-registration (Step 5).

## Verdict

**Fix-then-accept.** F1 (Medium) and F2 (Low) both applied in this pass.
