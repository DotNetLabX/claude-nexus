# F15-SkillCandidateMiner — Summary

Mode: architect-led fast lane

## Status: COMPLETE

## What Was Built

- `mine-skill-candidates` — the tenth mine: a code + git-history pattern miner (Lens A co-change,
  Lens B structural recurrence, hand-authored seed join, fresh-context skeptic + debt health
  gate) emitting skill candidates AND an `## Anti-patterns (do-not-propagate)` list into the
  `docs/skill-gaps/registry.md` species. `mine-skill-gaps` extended as canonical schema owner
  (9-column header with `source` after `kind`); family swept nine → ten across 8 files.

## Key Outcomes

- 1 new shipped skill + 8 modified skill files + 4 delivery artifacts (implementation, fixtures,
  review, lessons); golden-fixture run PASS on `omnivision-ai-sdk` (6/7 seeded recipes
  lens-corroborated with resolving commit/`file:line` citations; 1 anti-pattern row `PRC-9`,
  debt-row-backed).
- skill-lint clean on both edited skills; both acceptance greps exact (count-sweep 0 hits;
  `ninth mine` positive control = exactly `mine-skill-gaps/SKILL.md:20`); offline suite 587/587.
- Done-check: PASS after 1 fix cycle — cycle-1 FAIL on skill-conformance (2 mapped skills applied
  from memory, unlogged); cured by real Skill-tool invocations + honest self-report correction;
  the improve-skills re-walk surfaced and folded one real gap (AP6 — S3 emit now incremental).
- Release: one MINOR bump of `nexus` (applied at close via `--staged --minor`, plan D7).

## Deviations from Plan

- D2 fired (plan-sanctioned): Step 4 ran Follow-by-Read — the new skill isn't in the installed
  cache.
- D5 fired (plan-sanctioned): Code Maat/java unavailable — git-numstat co-change fallback,
  declared in the fixture registry.
- Anti-pattern substitution COR-4 → PRC-9 (tech-spec's own named-in-report allowance): COR-4 and
  IFC-4 were both resolved by the SDK's concurrent refactoring days before the run —
  skeptic-confirmed stale; PRC-9 is live and debt-row-backed.
- ADR renumber 65 → 66: the concurrent F3-AnalyticsBorrowWave close claimed ADR-65 (same day);
  register re-checked at write time per the ADR-28 discipline — no other change to the owed
  extraction.

## Notes

- The `.claude/audit/violations.log` entries naming `f15-developer` are false positives from the
  architect's qualifier-first dispatch name (recorded incident class; role hooks peel only
  role-prefixed names) — discounted in the done-check; lesson logged.
- Consumer handoff: the SDK's F2-SdkRewrite P3 consumes the shipped skill via a real run inside
  that repo (writing its actual `docs/skill-gaps/registry.md`) — operator-owed, deliberately not
  part of this feature (plan D1).
- Backlog row flip and omni twin sync sequenced after the concurrent F3 session's commit (shared
  `docs/backlog.md` / twin-repo surfaces).
