# F28-RegenerateUnit — Summary

Mode: architect-led fast lane

## Status: COMPLETE

## What Was Built
- `regenerate-unit` (nexus 1.43.0) — the program-home regeneration skill codifying the
  5×-validated wave inner loop: four fail-closed preflight gates (gated+fresh registry, F29-audited
  oracle, golden-seam catalog check, perf-delta audit), contract pack + owner shape directive,
  clean-room generation with declared ambiguities, ≤3 adjudicated repairs, PROVE via the
  mine-oracle-strength report seam, standalone adoption ask — plus cluster mode (G1 rule→target-home
  mapping, fail-closed on orphaned registries) and the G2/G3 gate artifacts the fix-shelf pilot
  needs. Calibration ledger seeded with the 4 ratified rulings; owner-ratified stage-model pins.

## Key Outcomes
- 1 skill created (no scripts — agent-orchestrated), program doc §5/§7 updated, MINOR bump
  1.42.2 → 1.43.0 + CHANGELOG.
- Lint fail 0 (developer ×2 + architect re-run); all 6 acceptance contracts re-verified
  independently at done-check.
- Plan review: code-grounded critic (opus) GO-with-fixes — its HIGH caught same-day program-doc
  drift (§7 rewritten by external sessions between F29's ship and F28's planning). Done-check:
  PASS, first cycle. Zero structural deviations.

## Deviations from Plan
- None structural. Lane-close steps (commit, twin sync) deferred by dispatch rule; §7 item-2
  softening declined (another session owns the LEAD narrative).

## Notes
- The fix-shelf cluster pilot (campaign #2) is now unblocked — F27 charter remains its other gate.
- Omni twin sync remains operator-owed (repo absent on this machine) — now covers 1.42.x + 1.43.0.
