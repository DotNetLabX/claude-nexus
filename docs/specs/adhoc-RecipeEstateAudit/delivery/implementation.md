# Recipe Estate Audit — Implementation

Implements the 6 confirmed-set steps in `delivery/plan.md`. Ad-hoc lane (no spec); done-check is
findings/ADR mapping. All Phase-1 findings were verified against live source before this phase and are
not re-derived here.

## Files Created
- `plugins/nexus/hooks/scripts/lib/is-code-file.js` — single shared `isCodeFile()` predicate for the
  three hooks. Canonical extension set = the union of the three prior copies; normalizes backslashes
  before the area/extension test (Step 4).

## Files Modified
- `plugins/nexus/skills/search-researches/SKILL.md` — (Step 1) rewrote the frontmatter `description` to
  the lean six-component rubric and stated the cluster-A boundary in one line (inline+captures vs the
  built-in `deep-research` report vs a bare generic agent); added a **Fallback when the forked researcher
  is unavailable** section (names `cite-check.mjs` by path; resolves the "researcher unavailable AND no web
  access" case = block, or answer cold at `Confidence: low` with no pool entry); re-labelled the three
  body references to `deep-research` from "external harness" to **built-in workflow (ADR-1)**.
- `plugins/nexus/skills/evaluate-skill/SKILL.md` — (Step 3, cluster C) description now states it is the
  DIAGNOSE half; order evaluate→improve; not interchangeable with improve-skills.
- `plugins/nexus/skills/improve-skills/SKILL.md` — (Step 3, cluster C) description now states it is the
  APPLY half; sibling of evaluate-skill; order line.
- `plugins/nexus/skills/boy-scout/SKILL.md` — (Step 3, cluster E) description adds the scope tie-breaker vs
  simplify (in-file+always-reports vs diff-scoped).
- `plugins/nexus/skills/diagnose/SKILL.md` — (Step 3, cluster F) description adds the timing line (after the
  first obvious fix fails, before the 3-attempt escalation).
- `plugins/nexus/hooks/scripts/guard.js` — (Step 4) removed the local `isCodeFile`; imports the shared lib.
- `plugins/nexus/hooks/scripts/pipeline-gate.js` — (Step 4) removed the local `isCodeFile`; imports the
  shared lib. **Behavior change:** now treats `.sh`/`.ps1` as source (it previously omitted them).
- `plugins/nexus/hooks/scripts/boundary-detector.js` — (Step 4) removed the local `isCodeFile`; imports the
  shared lib. Predicate-level normalization now present (was only at the call site).
- `tests/unit/pipeline-gate.test.mjs` — (Step 4) two new tests: `.sh`/`.ps1` now denied in analyze phase;
  a Windows backslash source path denied while backslash `docs/`/`.claude/` paths still pass.
- `tests/unit/boundary-detector.test.mjs` — (Step 4) two new tests: `.sh` logged for a non-code role; a
  Windows backslash source path logged while backslash doc/system paths are not.
- `docs/architecture/README.md` — (Step 5) ADR-4 preload table: `lessons-format` row corrected from
  `architect` to `architect, developer, reviewer` to match live frontmatter. Other four rows verified
  accurate.
- `docs/specs/adhoc-RecipeEstateAudit/delivery/selection-index.md` — (Steps 3-D + 6 reconcile) cluster-D
  resolution recorded (document on-demand, do not preload; named who reaches for each); routing-table rows
  L17/L18 and cluster A reconciled so breadth routes to the built-in `deep-research` and `deep-research` is
  re-labelled a Claude Code built-in (not external/OMC).
- `plugins/nexus/.claude-plugin/plugin.json` — (Step 6) version 1.13.1 → 1.13.2.
- `plugins/nexus/CHANGELOG.md` — (Step 6) 1.13.2 entry (auto-generated by bump-plugin).

## Key Decisions
- **Step 2 added nothing to `rules/`.** The plan's resolved default held: the three-tool tie-breaker (the
  incident's actual confusion) now lives in the Step-1 `search-researches` description — the always-on
  metadata layer agents already route on — so no always-on rules text was added. Net `rules/` line delta is
  **0** (research-before-asking=67, agents-workflow=215, total=282, unchanged), satisfying the binding accept
  that any net add regresses the very research-minimize-resident-text finding this pass acts on.
- **Step 4 unified predicate adopts guard.js's two behaviors as canonical:** backslash normalization at the
  predicate level, and the union extension set. Both other copies had drifted from guard.
- **`deep-research` is the breadth path.** With the Step-1 de-scope (below), a breadth/landscape scan no
  longer routes to a sanctioned `search-researches` fan-out mode; it routes to the built-in `deep-research`
  workflow. `search-researches` stays the single-fact, captures-to-the-pool path (its fan-out remains only a
  budget-gated option, never the sanctioned breadth mode).

## Skills Used

**Log reconciliation (read first).** The scoped skill-invocation log for this session (agent
`developer-audit-impl`) records exactly one Skill-tool invocation: **`implementation-format`** (loaded to
write this file). The plan's other mapped non-None skills — `improve-skills` (Steps 1, 3), `tdd` (Step 4),
`release-plugin` (Step 6) — do **not** appear in the log because they were applied via the **dev-repo
direct path**, not the Skill tool: this repo *builds* the plugin, so shipped skill source is edited
directly and each skill's **binding gate is run as a script**. The "invoked" column below reflects the log;
each direct-path row is marked a deviation with the binding-gate evidence that actually ran.

| Step | Skill(s) invoked (per log) | How applied + binding-gate evidence |
|------|----------------------------|--------------------------------------|
| 1 | None — deviation: `improve-skills` via dev-repo direct path | Shipped `search-researches/SKILL.md` edited DIRECTLY (not the Skill tool — the dev-repo carve-out). Binding gate = `skill-lint.mjs` → exit 0; description 729 chars (< 1024 ceiling). |
| 2 | None | plan: Skill None; doc/rules edits, net-resolved to a no-op (fix carried by Step 1's description). |
| 3 | None — deviation: `improve-skills` via dev-repo direct path | Four shipped `SKILL.md` (evaluate-skill, improve-skills, boy-scout, diagnose) edited DIRECTLY. Binding gate = `skill-lint.mjs` on all 4 → exit 0. |
| 4 | None — deviation: `tdd` applied directly | plan: Skill None (JS refactor), TDD yes. Red-green applied directly: +4 tests asserting the union extension set + Windows backslash path; full suite green (224/0). |
| 5 | None | plan: Skill None (doc edit). |
| 6 | None — deviation: `release-plugin` via dev-repo direct path | `bump-plugin.mjs` + `gen-omni.mjs` run DIRECTLY (release-plugin is a dev-repo script tool, not Skill-tool-invoked) → 1.13.1 → 1.13.2; `bump --check` and `gen-omni --check` both clean. gen-commands skipped — no agent file changed (plan-sanctioned). |

**Why direct-path, not Skill-tool invocation (deviation rationale).** Per CLAUDE.md, this is the
plugin-development repo: `improve-skills` and `release-plugin` are explicitly dev-repo tools that operate
on shipped source by running their scripts (`skill-lint.mjs`, `bump-plugin.mjs`), and `tdd` is a
red-green discipline, not a tool call. The deterministic binding gates (skill-lint exit 0, suite 224/0,
bump/twin --check clean) are the real conformance evidence and all ran; re-invoking the three via the
Skill tool would only manufacture log lines without changing the work or its verification.

## Carry-Over Findings
| Title | Severity | For | Evidence | Note |
|-------|----------|-----|----------|------|
| `node --test tests/unit/` shorthand fails on Node 24 | low | reviewer | `node --test tests/unit/` → MODULE_NOT_FOUND; `node --test "tests/unit/*.test.mjs"` is green | Plan/sharpening #1 used the bare-dir form. On this Node (v24.13.0) the test runner needs a glob, not a bare directory. Use the glob form to reproduce green. Not a code defect — a command-form note. |
| `bump --check` reports "no bump needed" post-bump | low | reviewer | `node scripts/bump-plugin.mjs --check` after the applied bump | This is the healthy post-bump state (version now current with shipped files), not a failure. CI `plugin-release-check.yml` would pass. |

## KB Changes
None. (No `docs/kb/` entries were created or modified — the cluster-D resolution and `deep-research`
re-label live in the audit's `selection-index.md`, not the KB.)

## Deviations from Plan
- **Step 1 — DROPPED the "promote breadth-first fan-out as a sanctioned mode" bullet (owner de-scope).**
  Per the Phase-1 owner decision carried into this phase: keep the description-tightening and the
  fallback-capture clause; drop only the fan-out-promotion bullet. The current "Default a single researcher"
  section was left framing fan-out as a budget-gated option (not promoted to a sanctioned breadth mode), and
  a one-line pointer added that breadth belongs to the built-in `deep-research`. This is the planned
  deviation, not an omission.
- **Step 6 — corrected stale plan premise (plan line 99).** The plan assumed an uncommitted in-flight
  1.13.1 to "fold into." Verified false: HEAD already released 1.13.1 and `git diff HEAD --
  plugins/nexus/.claude-plugin/plugin.json` is empty. There was nothing to fold into — the audit took a
  fresh PATCH bump → **1.13.2** with its own CHANGELOG entry. No in-flight diff was hunted.
- **Step 6 — selection-index + `/deep-research` reconciliations folded in (handed from adhoc-NexusResearch).**
  Routing-table rows and cluster A in `selection-index.md` described `search-researches` breadth-first
  fan-out as sanctioned; reconciled to route breadth to the built-in `deep-research`. Applied the
  `/deep-research` re-label: it is a Claude Code BUILT-IN workflow (user-invocable only, gated by CLI
  version/plan), not an external/OMC harness — in `selection-index.md` and in `search-researches/SKILL.md`.
- **Step 6 — gen-commands skipped (plan-sanctioned).** No `agents/*.md` changed (Step 2 touched only
  `rules/`; nothing else touched agents), so `gen-commands` was N/A per the plan's LOW-1 condition.
- **Step 4 — normalization drift accurately scoped.** The backslash drift is real at the predicate level,
  but was partially MASKED at call sites: `pipeline-gate.js:62` and `boundary-detector.js:121` already
  normalized `fp` before calling `isCodeFile`. Extraction still correctly collapses the drift (the predicate
  now normalizes for any future caller and the guard call-site path, which did not pre-normalize), and the
  documented observable change is therefore the union extension set (pipeline-gate gains `.sh`/`.ps1`); the
  normalization is a robustness consolidation, not a large observable behavior shift at the existing call
  sites. Enforcement consequence: pipeline-gate's analyze-phase + dev-source gates now fire on `.sh`/`.ps1`.

*Status: COMPLETE — developer, 2026-06-18*
