# Skill Estate Consolidation — Implementation

Worktree: `D:\src\claude-plugins\nexus-skillwt` on branch `adhoc-SkillEstateConsolidation` (sanctioned isolation, Q2). All plugin paths below are under this worktree; consuming-repo (Step 9) paths are under `D:\src\dotnet-microservices`.

## Files Created
- `plugins/nexus-dotnet/skills/add-state-machine/SKILL.md` — (Step 1) new shipped skill, pattern-first port of the local `article-state-machine`. Teaches data-driven state-transition validation for any write-side aggregate: cached `{Aggregate}StateTransition` seed table, `I{Aggregate}StateMachine` + `{Aggregate}StateMachineFactory` delegate, `ValidateStateTransition` guard as the first line of the state-mutation method, cache-backed and DbContext-backed DI variants. Articles material (`ArticleStage`, `ArticleActionType`, `ArticleStageTransition`, `Stateless` wrapper) cited only inside explicit `(reference app: ...)` exemplar clauses (D3 register). Reference-model provenance tags (`**domain-3**` etc.) dropped — mining artifacts with no shipped home. `user-invocable: true` added (estate convention for code-pattern skills; base lacked it). **[Fix Cycle 1]** dropped the dangling unused `Person editor` parameter from the `Approve` example (MEDIUM) and templated the two bare `IArticleAction<...>` occurrences to `I{Aggregate}Action<...>` (LOW) — see Fix Cycle 1 section below.
- `plugins/nexus-dotnet/skills/file-storage-patterns/SKILL.md` — (Step 2) new shipped skill, pattern-first port of the local `file-storage-patterns`. Teaches pluggable file-storage module composition: provider choice per service, generic options-marker subclass as the DI key (no keyed DI), singleton-default + scoped-per-extra registration, compensating `TryDeleteAsync` (file write outside the DB transaction), cross-stage byte migration in consumers, and the factory-vs-direct-injection variant decision. Description neutralized ("a file asset", not "article assets"). Submission/Review/Production/Article recast as `(reference app: ...)` exemplars; reference-model tags (`filestorage-1..4`, `modules-3`, `consumers-4`) dropped. Kept the `## Verify` deterministic post-condition greps (P1). Scope fence added (base had none) naming `consumer-patterns`/`add-integration-event`/`service-registration`.
- `plugins/nexus-dotnet/skills/file-storage-patterns/references/templates.md` — (Step 2) ported the lifted-code template file; header reframed as reference-app worked example (one covering exemplar clause), reference-model row-id tags stripped, code blocks kept verbatim as the copy-paste exemplar.
- `plugins/nexus-dotnet/skills/consumer-patterns/SKILL.md` — (Step 3) new shipped skill, pattern-first port of the local `consumer-patterns`. Teaches the five-phase MassTransit consumer shape, the three-variant idempotency decision table (silent skip / throw-if-exists / ExistsAsync-return), reference-data hydration (`GetOrCreate` + shadow-row pair), and the projection-vs-write-side split. Description delimited against `add-integration-event` (F6: this skill = author/edit an `IConsumer`; add-integration-event = propagate an event). Article/Journal/ArticleHub/Submission/Review/Production recast as `(reference app: ...)` exemplars; reference-model tags (`consumers-1..5`, `datasharing-2/4`, `boundaries-2`, `persistence-1`) dropped. **[Fix Cycle 1]** tidied the idempotency-table "article-creation consumers" cell to "aggregate-creation consumers (reference app: ...)" to match the parallel "Aggregate creation…" prose two lines below (review Open Question — confirmed as a tidy, not a real register violation).
- `plugins/nexus-dotnet/skills/consumer-patterns/references/write-side-consumer.md` — (Step 3) ported; reference-app source named in header, row-id tags stripped from code comments, code kept as exemplar.
- `plugins/nexus-dotnet/skills/consumer-patterns/references/projection-consumer.md` — (Step 3) ported; variant A (create-new) + variant B (update-existing) kept, reference-app framing.
- `plugins/nexus-dotnet/skills/consumer-patterns/references/reference-data-consumer.md` — (Step 3) ported; `{X}Created`/`{X}Updated` shadow-row pair, generic placeholders + reference-app source.
- `plugins/nexus-dotnet/skills/service-infra-conventions/SKILL.md` — (Step 4) new shipped skill, pattern-first port of the local `service-infra-conventions`. Ten cross-cutting-infra sections: segregated `IClaimsProvider`/`IRouteProvider` (only `HttpContextProvider` touches `HttpContext`), scoped `RequestContext` + correlation-id chain, fail-fast options binding, default-interface derivations, MediatR pipeline order, JSON casing, GlobalUsings grouping, private-field naming, framework-split validators. Description neutralized ("a .NET microservice", not "the Articles services"). Reference-model row-ids (`infra-1..5`, `appcqrs-2`, `namingx-3`, `conventions-1/2/5`) and the cheat-sheet "Row" column dropped; Source citations reframed as `(reference app)` and line numbers dropped (rot-prone). **Section 7 (CPM) slimmed to convention + pointer** — the `<PackageVersion>`/`<PackageReference>` XML recipe removed; CPM mechanics owned solely by `central-package-management` (AP3, Step 4 accept). Scope fence names `service-registration` (DI placement) and `central-package-management` (CPM mechanics).

## Files Modified
- `plugins/nexus-dotnet/skills/domain-patterns/SKILL.md` — (Step 1) added a two-line pointer at the end of the "State Machine Pattern" section (after line 229) directing readers to `add-state-machine` for the build recipe; the design-level placement decision stays here. Satisfies Step 1's scope-fence requirement.
- `plugins/nexus-dotnet/skills/add-integration-event/SKILL.md` — (Step 3) description delimited against `consumer-patterns` ("For the consumer body itself ... see consumer-patterns") and Step 3 of its recipe now points to `consumer-patterns` for the consumer body while `workflows/Consumer.md` keeps placement/discovery/naming. F6 bidirectional routing.
- `plugins/nexus-dotnet/skills/add-integration-event/workflows/Consumer.md` — (Step 3) slimmed from a full dual-template file (Simple + Rich consumer bodies with idempotency + `GetOrCreate` hydration) to the wiring minimum: placement, a bare class shape (empty `Consume` with numbered pointer comments), auto-discovery, naming, location. Full templates removed; authoring depth pointered to `consumer-patterns` (F6 — no two authoritative consumer templates coexist).
- `plugins/nexus-dotnet/skills/create-feature/workflows/EndpointCarter.md` — (Step 6 fold F-a) corrected a contradiction with the canonical `authorization-patterns`: the per-resource access check is a **declarative authorization requirement** stacked by `RequireRoleAuthorization`, not a check "inside the handler".
- `plugins/nexus-dotnet/skills/create-domain-event-handler/workflows/Handler.md` — (Step 6 fold F-c) added the cross-cutting-handler `EventHandlers/`-folder exception to the Location Rule (genuinely absent upstream; reference app: the `ArticleTimeline` family).
- `plugins/nexus-dotnet/skills/create-domain-event-handler/SKILL.md` — (Step 6 fold F-d) Step 6 now runs two wiring verify-greps (publisher registered, EF interceptor registered) before `dotnet build`.
- `plugins/nexus-dotnet/skills/create-grpc-contract/SKILL.md` — (Step 6 fold F-e) added a "Handler usage" section (the two sanctioned client-call shapes: lazy get-or-create hydration; authoritative gate before mutate; read-model registers zero clients) — the "how to actually use gRPC in a handler" the skill lacked. **[Fix Cycle 1]** genericized both code examples (were bare `_personRepository`/`_personService`/`GetPersonRequest`/`Person` and `_journalService`/`GetJournalRequest`/`article.JournalId`/`article.Approve` with zero exemplar framing — a D3 register violation, HIGH) to `_repository`/`_{serviceName}Service`/`{Request}`/`{Entity}`/`aggregate.ForeignId`, matching the skill's own `{Request}`/`{Response}`/`{ServiceName}` convention already used in `workflows/Contract.md`/`Server.md`/`Client.md`.
- `plugins/nexus-dotnet/skills/create-grpc-contract/workflows/Contract.md` — (Step 6 fold F-e) added the `[ProtoMember(n)]` **permanence** wire-compat rule (never renumber/reuse once shipped).
- `plugins/nexus-dotnet/skills/add-integration-event/workflows/Publisher.md` — (Step 6 fold F-f) added the "one handoff seam" rule: the publisher handler is the only sanctioned `_publishEndpoint.Publish` site.
- `plugins/nexus-dotnet/skills/add-integration-event/SKILL.md` — (Step 6 fold F-f) Step 4 now runs a Verify block (contract carries no domain type; publish only in the publisher handler; no manual consumer registration) before `dotnet build` (the skill previously had no Verify section).
- `plugins/nexus-dotnet/skills/persistence-patterns/SKILL.md` — (Step 6 fold F-g) added two configuration-ladder rungs — `EnumEntityConfiguration<T,TEnum>` (behavior-driving enum as a seeded queryable table, not `[Description]`+reflection) and `MetadataConfiguration<T>` (composite-key metadata table; cross-links `add-state-machine`) — plus the `MaxLength.C*`-constants-not-literals rule.
- `plugins/nexus-dotnet/.claude-plugin/plugin.json` — (Step 8) skill-count claim 33 → 37; version bumped 1.4.0 → **1.5.0** (MINOR, owner-banked — 4 new skills = new capability) via `bump-plugin.mjs --minor`.
- `plugins/nexus-dotnet/CHANGELOG.md` — (Step 8) `[1.5.0]` entry written describing the 4 ports + the re-registration + the fold-upstream + ADR-51 + count 33→37 (replaced the generated stub).
- `docs/skill-backlog.md` — (Step 8) added 4 `Skills Created` rows (the ports), 1 consolidated `Skills Fixed` row (Steps 5–6), and 1 `Deferred` row (the Step-6 residual fold-candidates, so the mining is preserved — D2). All sourced `adhoc-SkillEstateConsolidation`.
- `docs/architecture/README.md` — (Step 7) added **PROPOSED ADR-51** — "nexus-dotnet skills are pattern-first and exemplar-cited" — as both the index row (line 69, `*(PROPOSED — owner ratifies)*`, matching ADR-24's precedent) and the body record (Context/Decision/Why/Tradeoffs/Rejected), placed after ADR-50's body. Points back to the plan's Definition (D1–D3) as provenance. Retargeted to ADR-51 per Q1 (ADR-50 was already Accepted).
- `plugins/nexus-dotnet/skills/authorization-patterns/SKILL.md` — (Step 5) D3 re-registration. Description rewritten pattern-first ("Endpoint authorization for a role-based domain — a closed role enum...", was "for the article lifecycle"). Added a top-level `> Worked exemplar — the reference app` clause attributing all `UserRoleType`/`Role`/`RequireRoleAuthorization`/`IArticleAccessChecker`/`ArticleRoleRequirement`/`ArticleActors`/ArticleHub/service names to the reference app. Genericized pattern-level prose ("does this caller reach this article?" → "this resource?"; the ArticleHub read-model example framed `(reference app: the ArticleHub service ...)`). Reheaded "Source Files" → "Source Files (reference app)". **Content facts unchanged** (per Step 5 — only the register changed).

## Key Decisions
- **Reference-app citation style** matches the estate convention `(reference app: ...)` already used across shipped skills (`create-feature`, `create-grpc-contract`, `create-domain-event-handler`, `error-handling`, `service-registration`) rather than inventing a new exemplar-clause syntax.
- **Generic placeholder scheme:** `{Aggregate}` / `{Aggregate}State` / `{Aggregate}ActionType` / `{Aggregate}StateTransition`, consistent with `domain-patterns`' existing `{Aggregate}StageTransition`/`{Aggregate}StateMachineFactory` placeholders (only "Stage" → "State" generalized, since a state machine validates *state* transitions and "Stage" is the reference app's article-specific word).
- **Judgment Gate run inline** (main-context rubric pass, Layers 1–4) per the plan's binding acceptance ("an inline rubric judgment pass (Layers 1–4, no Critical/High)"), not by spawning `evaluate-skill`. The plan explicitly sanctions the inline form; spawning a quality-eval agent per skill (×4) is neither required by the plan nor appropriate for a developer subagent under the no-spawn rule. Rubric read from `evaluate-skill/references/rubric.md` to ground the inline pass.

## Step 6 — Fold-Upstream Disposition Table (binding artifact)

Ten pairs (local → shipped) diffed claim-by-claim (enumeration delegated to 4 read-only research agents; folds/dispositions decided here). **Overall verdict: the 1.4.0 fold held — the overwhelming majority is `already-upstream`.** Genuinely-missing, portable, high-value gaps were folded (targets named below); repo-lifecycle/domain-specific detail was dropped; a few genuine-but-larger patterns were deferred to the backlog so the mining is preserved (D2). The two contradictions both resolve as **shipped is the corrected form** — the local's stale wording is superseded, NOT re-folded.

| Pair (local → shipped) | Verdict | Folded (→ target file:section) | Dropped / Deferred (why) |
|---|---|---|---|
| 1. add-integration-event → same | mostly already-upstream | **F-f:** exclusive-publish-site rule → `add-integration-event/workflows/Publisher.md` (top "One handoff seam"); Verify greps (no domain-type in contract / single publish site / no manual consumer reg) → `add-integration-event/SKILL.md` Step 4 | DTO-name list, global-usings count, "namespace≠folder" (already in EventContract.md) — dropped as repo-specific/already-upstream |
| 2. error-handling → same | mostly already-upstream | none folded | repository `*OrThrow` family, "two not-found spellings" discipline, DomainException-subclass-needs-no-arm — **deferred to backlog** (genuine but error-handling is already comprehensive; low-risk future fold). Verbatim exception source, adapter-name lists — dropped (illustrative/repo-specific) |
| 3. create-aggregate → same (+domain-patterns) | mostly already-upstream; **1 contradiction** | **F-g** (shared with pair 7): EnumEntity config rung → `persistence-patterns/SKILL.md` "Enum-backed metadata" | **C1 (VO ctor):** local's absolute "private ctor" is SUPERSEDED by shipped's census-based "private default (~14/17) + sanctioned `internal`+`[JsonConstructor]` minority" (`ValueObject.md:13-19`, `domain-patterns:70-75`) — **not re-folded** (local is stale). Variant-decision heuristic, AddDomainEvent-exclusivity, Domain-layer verify-greps — deferred to backlog. Template packaging, registry-citation mechanism — dropped |
| 4. create-feature-slice → create-feature (+cqrs-patterns) | mostly already-upstream; **1 shipped-bug fixed** | **F-a:** resource-check-location correction → `create-feature/workflows/EndpointCarter.md` Notes (was "inside the handler"; now the declarative requirement — matches canonical `authorization-patterns`) | Mapster inheritance/VO-factory idioms, `[AllowFileUploads]`, `[AsParameters]` query binding — deferred to backlog (real but niche). FE `Configure()` permissiveness, service-name assignments, shared-base bodies — dropped (stack-neutral skill is legitimately more permissive / repo-specific) |
| 5. create-service → same | mostly already-upstream (much correctly relocated to service-registration / persistence-patterns / redis-patterns / central-package-management) | none folded | two layering verify-greps, Postgres snake_case rewriter, Redis Domain-csproj branch, healthchecked `depends_on` — deferred to backlog. "MUST dispatch domain events" over-generalization (read-model exception) — minor divergence, not folded. Port numbers, filename-typo note, precedent lists — dropped (repo-lifecycle) |
| 6. grpc-communication → create-grpc-contract | mostly already-upstream | **F-e:** two handler-usage patterns (lazy hydration; authoritative gate) + read-model-registers-zero-clients → `create-grpc-contract/SKILL.md` "Handler usage"; ProtoMember **permanence** rule → `create-grpc-contract/workflows/Contract.md` Rules | "4 of 6 call sites pass the token" — dropped (repo-lifecycle snapshot); "never new a GrpcChannel / cert bypass" — deferred (low) |
| 7. persistence-patterns → same | mostly already-upstream (Redis variant fully via `redis-patterns`); **1 contradiction (framing)** | **F-g:** `EnumEntityConfiguration<T,TEnum>` + `MetadataConfiguration<T>` ladder rungs + `MaxLength.C*`-not-literals rule → `persistence-patterns/SKILL.md` "Entity Configuration" | **C3 (UpsertAsync):** local is *descriptive* of old `SetValues(object)` code; shipped *prescribes* the `PropertyValues` overload fix (`persistence-patterns:22-28`) — shipped is correct, **not re-folded**. Caching implementation cluster (`ICacheable`/`GetOrCreateByType`/`CachedRepository`/`DatabaseCacheLoader`/`UnTrackCacheableEntities`) — **deferred to backlog** (concept is upstream; impl cluster is a coherent future fold). `{Svc}DbContext`-not-`AppDbContext`, no-per-repo-interface, table-naming — deferred. Audit-column internals, `DeleteByIdAsync` template — dropped |
| 8. domain-event-wiring → create-domain-event-handler | near-total already-upstream | **F-c:** cross-cutting `EventHandlers/` exception → `create-domain-event-handler/workflows/Handler.md` Location Rule; **F-d:** publisher+interceptor verify-greps → `create-domain-event-handler/SKILL.md` Step 6 | harvest-loop building-block code, `DomainEvent(action)` base example, "killed absolute eventpub-3" meta-note, project-skill lessons block — dropped (Blocks.* internals / lessons-tracking) |
| 9. authorization-security → authorization-patterns | **near-total already-upstream** (shipped is a domain-neutralized superset; adds material the local lacks) | none folded | JWT `RequireHttpsMetadata=false`/`SaveToken=true` knobs, Minimal-API filter `CreatedById==default` guard — deferred to backlog (low). Verbatim class bodies, `[Description]` attrs, full `IClaimsProvider` — dropped (already-upstream as prose/comments / infra out-of-scope). **No contradictions.** |
| 10. service-infra-conventions → service-registration (+central-package-management) | **~100% covered** | **folded (Step-4 port):** essentially every item now lives in the new shipped `service-infra-conventions` (Step 4); registration *locations* already-upstream in `service-registration`; CPM *mechanics* already-upstream in `central-package-management` | `BaseValidator` commented-out-logging caveat — dropped (one reference-app class's dead code). Reference-model row-ids, exact counts, source line numbers — dropped (intentional de-specialization on the Step-4 port). **No contradictions.** |

**Deferred-to-backlog items are recorded in `docs/skill-backlog.md` (Step 8) so the mining is not lost (D2)** — each is a genuine but lower-priority/larger fold, not a drop. Every touched shipped skill re-lints exit 0 (verified above).

## Skills Used
| Step | Skill(s) invoked | Notes |
|------|------------------|-------|
| 1 | improve-skills (scaffold path, dev-repo carve-out) + inline rubric judgment (Layers 1–4) | new shipped skill authored directly in `plugins/`; lint exit 0; rubric ACCEPT no Critical/High |
| 2 | improve-skills (scaffold path) + inline rubric judgment (Layers 1–4) | SKILL.md + references/templates.md; lint exit 0; description 'article'-free (smoke test); rubric ACCEPT |
| 3 | improve-skills (scaffold path + consolidating fix on add-integration-event) + inline rubric judgment (Layers 1–4) | SKILL.md + 3 references; F6 slim of add-integration-event Consumer.md; lint exit 0 on all touched; rubric ACCEPT |
| 4 | improve-skills (scaffold path) + inline rubric judgment (Layers 1–4) | SKILL.md; CPM section slimmed to pointer (AP3); lint exit 0; description 'article'-free; rubric ACCEPT |
| 5 | improve-skills (consolidating-fix path) | authorization-patterns re-registered (lint exit 0, description pattern-first); create-domain-event-handler sweep = no-op (only `article` token is `ArticleHub` inside its "Reference-app resolution" table). Shipped-skill fix: lint is the done-condition, no Judgment Gate. |
| 6 | improve-skills (consolidating-fix path across touched skills) | 10-pair fold-upstream diff; 7 fold-groups landed (F-a,F-c,F-d,F-e,F-f,F-g) into add-integration-event, create-feature, create-domain-event-handler, create-grpc-contract, persistence-patterns; disposition table written; lint exit 0 across all touched. Contradictions resolved as shipped-is-correct (no re-fold). |
| 7 | None (plan: Skill none — ADR authoring has no skill) | PROPOSED ADR-51 authored in docs/architecture/README.md (index + body); grep `PROPOSED` on new row = 1 hit |
| 8 | release-plugin (Follow) | full-estate lint exit 0 (65 folders); `node --test` 484 pass / 0 fail; count 33→37; backlog updated; `bump-plugin.mjs --minor` → 1.5.0; CHANGELOG written; `bump-plugin.mjs --check` exit 0. Commit + gen-omni + twin = team lead (developer never commits). |
| 9 | None (plan: Skill none) | **NOT executed — blocked by precondition.** Step 9 requires the 1.5.0 release to have shipped AND the consuming repo to have run `/plugin update` (installed ≥ 1.5.0). Neither can happen until the team lead commits the release. Deferred to after the release ships. |

## Fix Cycle 1 (Reviewer Findings — Step 2 review.md, Cycle 1/3)

Reviewer verdict: REQUEST CHANGES (1 HIGH, 1 MEDIUM, 1 LOW), plus one Open Question. All three findings addressed; the Open Question tidied.

| Finding | Severity | File | Fix applied |
|---|---|---|---|
| `create-grpc-contract`'s new "Handler usage" section reintroduces the D3 register defect (bare `Person`/`Journal`/`GetXRequest` domain vocabulary, zero exemplar framing) | HIGH | `plugins/nexus-dotnet/skills/create-grpc-contract/SKILL.md:36-50` | Genericized both code examples to `_repository`/`_{serviceName}Service`/`{Request}`/`{Entity}`/`aggregate.ForeignId` — matches the skill's existing `{Request}`/`{Response}`/`{ServiceName}` placeholder convention (`workflows/Contract.md`, `Server.md`, `Client.md`). Chose genericizing over exemplar-tagging (the reviewer's other offered option) because it keeps the section pattern-first with no added framing overhead, consistent with how the rest of the file already reads. |
| `add-state-machine`'s ported `Approve` example has a dangling unused `Person editor` parameter (the port dropped the one line that used it) | MEDIUM | `plugins/nexus-dotnet/skills/add-state-machine/SKILL.md:205-210` | Dropped the `editor` parameter from the signature; added a one-line comment marking where an actor/provenance-recording line goes for services that track it (mirrors the file's existing cache-loader caveat style at line 177), naming the reference app's `Article.Approve`/`ArticleActor` as the concrete example. |
| `add-state-machine` leaves `IArticleAction<...>` and `Person` bare, outside its own exemplar clause's stated `Article*` scope | LOW | `plugins/nexus-dotnet/skills/add-state-machine/SKILL.md:13,185,205` | Templated both `IArticleAction<{Aggregate}ActionType>` occurrences (lines 185, 205) to `I{Aggregate}Action<{Aggregate}ActionType>`, consistent with the file's existing `{Aggregate}StateMachineFactory`/`{Aggregate}ActionType` convention. The bare `Person` token was removed as a side effect of the MEDIUM fix (the parameter it belonged to was dropped). No clause-wording edit was needed — the general `{Aggregate}` placeholder (already documented in the top clause) now covers the templated type. |
| Open Question — `consumer-patterns/SKILL.md:32` table cell "article-creation consumers" vs the parallel prose "Aggregate creation…" two lines later | n/a (Open Question, confidence 65/100) | `plugins/nexus-dotnet/skills/consumer-patterns/SKILL.md:32` | Confirmed as an inconsistency worth tidying (not a real register violation — the cell sits in a column that legitimately names concrete reference-app services). Changed to "aggregate-creation consumers (reference app: Review, ArticleHub first stage)", matching the prose and the file's `(reference app: ...)` tagging convention used elsewhere. |

**Verification after fixes:** `skill-lint` re-run on all 3 touched skills (`create-grpc-contract`, `add-state-machine`, `consumer-patterns`) — all `OK`. Full-estate lint re-run (65 folders, all 4 plugins) — clean, exit 0. `node --test tests/lint/*.test.mjs tests/unit/*.test.mjs` re-run — 484/484 pass, 0 fail (no regression). Grep-verified: zero remaining `_personRepository|_personService|GetPersonRequest|_journalService|GetJournalRequest|article\.Approve|article\.JournalId` in `create-grpc-contract/SKILL.md`; zero remaining `Person editor` and zero remaining bare `IArticleAction` in `add-state-machine/SKILL.md`.

## Carry-Over Findings
| Title | Severity | For | Evidence | Note |
|-------|----------|-----|----------|------|
| Judgment Gate run inline, not via `evaluate-skill` agent | low | reviewer | plan Binding line: "an inline rubric judgment pass (Layers 1–4, no Critical/High)" | The plan explicitly sanctions the inline form; each new skill got a documented Layers-1–4 pass (rubric read from `evaluate-skill/references/rubric.md`). Verify against the rubric if desired. |
| Step-6 fold judgment (fold vs defer vs drop) | low | reviewer | implementation.md Step-6 disposition table | Enumeration was delegated to read-only research agents; fold/defer/drop calls were mine. Deferred items are recorded in `docs/skill-backlog.md` so nothing is lost (D2). Two contradictions resolved as shipped-is-correct (local stale wording not re-folded). |
| `authorization-patterns` body still carries `Article*` identifiers | low | reviewer | D3 register rule (F5): repo vocab allowed inside a named-exemplar clause | A top-of-skill "Worked exemplar — the reference app" clause covers the concrete identifiers; pattern-level prose was genericized ("this resource", not "this article"). Description leads with the pattern (smoke test clean). |

## Deviations from Plan

1. **Step 8 commit + `gen-omni` + twin commit NOT performed (developer boundary).** The plan Step 8 lists "Bump in the same commit … then `gen-omni.mjs` + `--check` … twin commit". The developer **never commits** (hard rule) and the release-plugin skill itself leaves edits staged-ready for the committer. `gen-omni` was also **not** run: it writes the omni twin from `plugins/**`, and running it from this feature worktree (branch `adhoc-SkillEstateConsolidation`) would generate a twin missing the concurrent `adhoc-MineVerifyPhpAdapter` pipeline's plugin state in the sibling `nexus` checkout — risking a clobber. **OPERATOR / TEAM-LEAD ACTION REQUIRED:** commit the plugin-repo change (all Files Modified above, `git add` the 4 new skill dirs) as ONE commit including the 1.5.0 bump; then run `node scripts/gen-omni.mjs` on the merged main + `node scripts/gen-omni.mjs --check` (exit 0) and commit the omni twin with a mirrored subject (`feat(adhoc-SkillEstateConsolidation): sync skill estate consolidation (omni 1.x.y)`).

2. **Step 9 (consuming-repo retirement) NOT executed — blocked by its own precondition (by design, critic F2).** Step 9 requires the 1.5.0 release to have shipped AND `D:\src\dotnet-microservices` to have run `/plugin update` (installed nexus-dotnet ≥ 1.5.0). Retiring the locals before that would leave the consuming repo skill-less in the gap. Neither the release nor the install can happen until the team lead commits (deviation 1). **Step 9 must run as a follow-up after the release is installed** — its acceptance greps (the §14 before/after regex gates from Q3, the 13-folder deletion, the backlog flips) are ready to execute exactly as the plan specifies.

*Status: DEVELOPER ROUND COMPLETE — Steps 1–7 fully done; Step 8 developer-editable parts done (lint/tests/count/backlog/bump 1.5.0, `--check` exit 0); Fix Cycle 1/3 applied (1 HIGH, 1 MEDIUM, 1 LOW + 1 Open Question, all resolved, re-lint + full test suite green, no regressions). Step 8 commit + `gen-omni` + twin, and all of Step 9, are team-lead/release-gated (see Deviations) — developer, 2026-07-07*
