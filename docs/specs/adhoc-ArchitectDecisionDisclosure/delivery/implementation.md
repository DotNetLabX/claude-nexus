# Architect Decision Disclosure — Implementation

## Files Modified
- `plugins/nexus/skills/create-implementation-plan/references/plan-template.md` — **Step 1.** Two edits: (1) section-map line (line 5) now lists `## Decisions` between `## KB Impact` and `## Open Questions` so the map order mirrors template order; (2) added a `## Decisions` template-body section immediately before `## Open Questions`, carrying the binding row-shape (decision · why · rejected alternative · status `decided | deferred`), the row bar (user-divergence AND two-way door; one-way door → ask-first only), the trichotomy fold (open stays in `## Open Questions`), the always-present rule with the exact explicit-empty sentence `None — no self-resolved calls met the disclosure bar`, and the additive/no-retro-flag clause.
- `plugins/nexus/agents/architect.md` — **Step 2.** Three edits: (1) step-13 auto-approve message (line 218) extended with the `Decisions taken: {M} — see ## Decisions` metric, plus prose fixing this message as the metric's single authoritative home (no Phase-2 checkpoint report; do not mutate the shared headline-metrics placeholder; standalone closes the plan-approval message the same way) — resolves critic HIGH; (2) new Plan Writing Rules bullet "Declare the decisions you resolved alone (`## Decisions`)" stating every plan carries the section, a call clearing the ADR-25 bar (user-divergence AND two-way door) gets a row at resolution time, one-way doors/open items route elsewhere, section always present with the explicit-None sentence, absent/silent-empty is a critic + done-check defect; (3) new Done-Check "Plan-hygiene check" one-liner (line 275): confirm `## Decisions` exists and is non-silent, else a plan-hygiene finding self-attributed to the architect's plan — does NOT Fail the developer or alter the step-disposition verdict. **Fix Cycle 1/3 (Codex cross-check, MINOR consistency):** appended `Plans that **predate** the section are exempt — do not flag them.` to the same line, matching the predate exemption already carried by `plan-template.md:83` ("plans predating this section are not retro-flagged") and `critic.md:49` ("plans that predate the section are exempt"). The done-check line was the one place the three-way exemption wasn't yet consistent.
- `plugins/nexus/agents/critic.md` — **Step 3.** Added item 6 to the Mode-2 "Plan reviews" checklist (after step ordering, line 49): a plan under review lacking the `## Decisions` section, or empty without the exact `None …` sentence, is a **MEDIUM** silent-decision-hygiene finding; plans that **predate** the section are exempt — flag only the plan under review, never referenced historical plans. Written as one physical line carrying all three binding tokens (`## Decisions`, `MEDIUM`, `predate`) so the acceptance grep is window-independent.
- `plugins/nexus/.claude-plugin/plugin.json` — **Step 4.** Version 1.24.0 → **1.25.0** (owner-escalated MINOR, user-ratified 2026-07-06; new user-facing capability).
- `plugins/nexus/CHANGELOG.md` — **Step 4.** Prepended a `## [1.25.0] — 2026-07-06` entry naming the Decisions mechanism (`## Decisions` section shape, `Decisions taken: N` metric, MEDIUM critic finding).
- `plugins/nexus/commands/architect.md`, `plugins/nexus/commands/critic.md` — **Step 4.** Regenerated by `scripts/gen-commands.mjs nexus` from the edited agent files (generated artifacts — not hand-edited). The other 6 command files were re-emitted byte-identical (no diff vs HEAD) and are not staged. **Re-regenerated after the Cycle 1/3 fix** (architect.md changed again) — re-ran `gen-commands.mjs nexus`; only `commands/architect.md` changed this time (`commands/critic.md` was already current from the prior run), re-staged both.

## Key Decisions
- Step 1 template-body section rendered as a single smoothed prose paragraph (developer's-call per plan's Binding vs developer's-call split) matching the terse voice of the neighboring `## KB Impact` / `## Open Questions` template sections, rather than a bulleted example. All binding tokens are present verbatim.
- **Step 4 bump was scoped with `--staged`, not run whole-tree.** The working tree carries a concurrent feature (adhoc-DotnetFeedbackApply): 28 dirty `plugins/nexus-dotnet/**` files plus an already-modified nexus-dotnet plugin.json (1.4.0) + CHANGELOG. `bump-plugin.mjs --minor` escalates *every* changed plugin (script lines 166-173), so a whole-tree run would have wrongly bumped nexus-dotnet 1.4.0→1.5.0 and hijacked that feature's release. Staged only the 7 nexus files and ran `--minor --staged`, which classifies staged-only paths → bumped nexus alone. Confirmed nexus-dotnet plugin.json stayed at 1.4.0.

## Skills Used
| Step | Skill(s) invoked | Notes |
|------|------------------|-------|
| 1 | None | plan: `Skill: None`, `TDD: no` — agent/skill prose editing, no pattern skill |
| 2 | None | plan: `Skill: None`, `TDD: no` — agent-doc prose editing, no pattern skill |
| 3 | None | plan: `Skill: None`, `TDD: no` — agent-doc prose editing, no pattern skill |
| 4 | release-plugin | plan: `Follow release-plugin` — skill invoked before running the bump/regen/validate flow |

## Carry-Over Findings
| Title | Severity | For | Evidence | Note |
|-------|----------|-----|----------|------|
| Concurrent nexus-dotnet feature now STAGED in the shared index | high | team-lead / reviewer | During the Cycle 1/3 fix, `git diff --cached --name-only` showed 28 `plugins/nexus-dotnet/**` files + `adhoc-DotnetFeedbackApply/delivery/{communication-log,implementation,lessons,review,review-codex,summary}.md` **already in the index** — staged by that concurrent pipeline between the initial Step 4 run and this fix round, not by me (I only ran `git add` on my own 7 files each time). HEAD/branch re-checked and unchanged (`main` @ `3a135a8`); nexus-dotnet plugin.json stayed at 1.4.0 in both the staged and working-tree view — no version collision, just an overlapping index. | **Escalated from "dirty" to "staged."** A bare `git commit` right now would sweep the concurrent feature's 28+ files into this feature's commit. Team lead's closure commit must scope explicitly to the 7 nexus files (`git commit plugins/nexus/... docs/specs/adhoc-ArchitectDecisionDisclosure/...` or equivalent pathspec / partial-index commit) — do not `git add -A` and do not run a bare `git commit` against the current index as-is. |
| Omni-twin sync deferred to team-lead closure | info | team-lead | Per spawn handoff: omni twin commit is a closure action; its footer pins the impl commit sha | `gen-omni.mjs` was NOT run. Run it after the nexus commit lands so the twin's `Generated from the nexus plugin (nexus {sha})` footer pins the real sha. |

## Deviations from Plan
- **Step 4 omni sync not run by developer (plan Step 4 text mentions the omni sync commit).** Per the spawn handoff, the nexus commit and the omni twin commit are team-lead closure actions (the twin footer must pin the implementation commit sha), so I stopped staged-ready without committing or running `gen-omni.mjs`. This matches the plan's own Step 4 acceptance note ("the omni twin commit … is delegated to the release-plugin flow; the done-check must not expect to observe it here").
- **Step 4 bump scoped via `--staged` rather than the plan's plain `--minor`.** Reason: concurrent nexus-dotnet dirt in the shared tree would otherwise be swept into the minor bump (see Key Decisions). Net effect on nexus is identical (1.24.0 → 1.25.0); the scoping only prevents collateral damage to the other feature. Same-commit rule (ADR-9) still satisfied — all nexus content + bump + CHANGELOG + regenerated commands are staged together.

*Status: COMPLETE — developer, 2026-07-06*
