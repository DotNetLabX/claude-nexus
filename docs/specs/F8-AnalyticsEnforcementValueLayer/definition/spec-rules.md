Spec-stamp: docs/specs/F8-AnalyticsEnforcementValueLayer/definition/w3-value-layer-spec.md @ sha256:2080af79d7ad (2026-07-18)

**Run metadata** — mode `mine-from-spec` (spec arm, Mine→Verify only). 3 clean-room miners (sonnet) produced 66 / 42 / 55 raw rules. Consolidate + skeptic pass by opus, 2026-07-18. Consensus rules below are merged at the semantic level; `Merged from:` names the contributing miner rules. Verification is against the spec text only.

**Counts** — 65 consensus rules + 2 delta rules (SF-3 fold, 2026-07-18) = 67. Consensus (65 mined): 63 at 3/3, 1 at 2/3, 1 at 1/3. Verdicts: 65 verified, 2 ambiguous. Killed: 2. Spec findings: 3 (1 by-design, 2 folded → SR-30 / SR-66-67). Citation fixes: 1.

**Delta re-check** (2026-07-18, spec sha256:2080af79d7ad) — SF-2 and SF-3 were folded into the spec the same day. This is a DELTA pass, not a re-run: SR-30 re-verified with a stock-parser parse floor as its boundary; delta rules SR-66/SR-67 added for the SF-3 fold; Spec findings dispositioned. All other rows unchanged.

## Rules

| SR | ruleName | kind | statement | boundary | verbatim citation | layer | crit | consensus | triage | verdict | reason |
|---|---|---|---|---|---|---|---|---|---|---|---|
| SR-1 | delivery-skill-first | boundary | W3 ships the value-layer capability as a gated `value-briefing` skill in nexus-analytics. | gated skill, not an agent | "Delivery shape: skill-first — a gated `value-briefing` skill in nexus-analytics." | skill | golden | 3/3 | transcribed | verified | |
| SR-2 | no-intelligence-analyst-agent | negative | No intelligence-analyst agent persona ships in this slice. | no agent persona this slice | "No intelligence-analyst agent persona." | skill | golden | 3/3 | transcribed | verified | |
| SR-3 | first-consumer-product-repo-only | boundary | The omnishelf-analytics product repo is the only consumer wired in this slice. | product repo is the sole consumer | "First consumer: the omnishelf-analytics product repo only" | process | core | 1/3 | interpretive | verified | |
| SR-4 | web-ask-pure-accuracy-surface | negative | KG's web-ask stays a pure accuracy surface, serving no value content. | web-ask serves no value content | "KG's web-ask stays a pure accuracy surface; value content never enters `seed/db/` grounding." | data-contract | golden | 3/3 | transcribed | verified | |
| SR-5 | value-content-never-in-seed-db-grounding | negative | Value content never enters `seed/db/` grounding. | all of seed/db/ excluded from value content | "value content never enters `seed/db/` grounding." | data-contract | golden | 3/3 | transcribed | verified | |
| SR-6 | value-model-home-governed | boundary | The value-model artifact's home is KG `docs/value-model/`; KG owns it. | home = docs/value-model/, KG-owned | "Value-model artifact home: KG `docs/value-model/`, governed — KG owns it" | data-contract | golden | 3/3 | transcribed | verified | |
| SR-7 | value-model-edits-governed-only | gate | Edits to the value model land only via governed runs (ledger entry → run disposition → value model). | edits only via governed runs | "edits land only via governed runs (ledger entry → run disposition → value model)" | process | core | 3/3 | transcribed | verified | |
| SR-8 | sync-one-way-kg-to-product | negative | The value model syncs one-way KG → product; never product → KG. | KG→product only | "KG → product one-way, like the bundle sync; never product → KG" | sync | golden | 3/3 | transcribed | verified | |
| SR-9 | vwh-historical-not-live | boundary | VWH remains a historical source, not a live upstream feed. | VWH = historical, not live | "VWH remains a historical source, not a live upstream." | process | core | 3/3 | transcribed | verified | |
| SR-10 | no-new-feature-slug | boundary | W3's spec home is the existing F8 lane; no new feature slug is created. | stays in F8, no new slug | "Spec home: this F8 lane — no new feature slug." | process | edge | 3/3 | transcribed | verified | |
| SR-11 | no-additive-msm-contract-constructs | negative | No additive optional constructs land on the `mine-semantic-model` output contract; it is untouched by W3. | msm output contract untouched | "NO additive optional constructs land on the `mine-semantic-model` output contract" | data-contract | golden | 3/3 | transcribed | verified | |
| SR-12 | no-consuming-validator-compat-rule | negative | No consuming-validator compatibility rule is needed or added. | no compat rule added | "NO consuming-validator compatibility rule is needed" | data-contract | edge | 3/3 | transcribed | verified | |
| SR-13 | pool-classification | invariant | Each KPI row carries a pool of A, B, or E; composites are allowed. | pool ∈ {A,B,E}, composites OK | "**pool** (A / B / E, composites allowed — operational, monetizable, enabler)," | data-contract | core | 3/3 | transcribed | verified | |
| SR-14 | value-weight-provenance-revisions | invariant | Each value weight carries provenance-adjusted revisions with their evidence (e.g. labor_yield 0.9→1.4). | revisions carried with evidence | "**value weight** incl. provenance-adjusted revisions (labor_yield 0.9→1.4 with its evidence)," | data-contract | core | 3/3 | transcribed | verified | |
| SR-15 | sap-mapping-where-sourced | invariant | A row carries an SAP displacement mapping (field + Fiori app) only where one is sourced. | SAP field + Fiori app, where sourced | "**SAP displacement mapping** (field + Fiori app) where sourced," | data-contract | core | 3/3 | transcribed | verified | |
| SR-16 | coefficient-sources-with-guardrails | invariant | Elasticity/coefficient sources S001–S007 are carried WITH both guardrail classes. | S001–S007 + both guardrail classes | "**elasticity/coefficient sources** (S001–S007) carried WITH their guardrails" | data-contract | golden | 3/3 | transcribed | verified | |
| SR-17 | dimensional-plausibility-bound | boundary | Dimensional plausibility bounds a 1pp OOS reduction to fractions of a percent of category revenue, never 20%. | fractions of a percent, never 20% | "a 1pp OOS reduction is worth fractions of a percent of category revenue, never 20%" | data-contract | golden | 3/3 | transcribed | verified | |
| SR-18 | confounds-to-encode-list | invariant | The confounds-to-encode list covers phantom inventory, substitution, promo, and seasonality. | four named confounds | "the confounds-to-encode list (phantom inventory, substitution, promo, seasonality)," | data-contract | core | 3/3 | transcribed | verified | |
| SR-19 | provenance-schema-v2-grammar | invariant | Provenance follows the schema-v2 grammar: cited origins and truthful `verified` dates. | schema-v2 grammar | "**provenance** in the schema-v2 grammar — cited origins, truthful `verified` dates," | data-contract | golden | 3/3 | transcribed | verified | |
| SR-20 | no-scalar-confidence-values | negative | Provenance carries no scalar confidence values. | scalar confidence forbidden | "no scalar confidence values." | data-contract | golden | 3/3 | transcribed | verified | |
| SR-21 | join-by-construct-id | invariant | A row joins to the KG semantic model by construct id (measureId/termId) where one exists; by VWH KPI id otherwise. | construct id first, VWH id fallback | "**Joined to the KG semantic model by construct id** where one exists (measureId / termId); by VWH KPI id otherwise" | data-contract | core | 3/3 | transcribed | verified | |
| SR-22 | unmapped-rows-legal-if-marked | boundary | Rows with no KG construct are legal only if explicitly marked so. | no-construct rows legal iff marked | "rows with no KG construct are legal and explicitly marked so." | data-contract | core | 3/3 | transcribed | verified | |
| SR-23 | fail-closed-construct-id-resolution | gate | Every referenced construct id must resolve against the bundle (fail-closed). | unresolved id → validation fails | "every referenced construct id must resolve against the bundle" | data-contract | golden | 3/3 | transcribed | verified | |
| SR-24 | provenance-grammar-rules-apply | invariant | The provenance-grammar rules are enforced on the value model at validation. | grammar enforced on the value model | "the provenance-grammar rules apply." | data-contract | core | 2/3 | interpretive | verified | |
| SR-25 | validation-failure-blocks-run | gate | A validation failure blocks the run that produced the artifact. | failure blocks producing run | "A validation failure blocks the run that produced it." | data-contract | golden | 3/3 | transcribed | verified | |
| SR-26 | value-model-outside-seed-db | boundary | The value model lives outside `seed/db/` (hard boundary — everything under seed/db/ is served as grounding). | outside seed/db/ | "**Home: KG `docs/value-model/`** — outside `seed/db/` (hard boundary: everything under `seed/db/` is served recursively as answer grounding by the KG ask path)." | data-contract | golden | 3/3 | transcribed | verified | |
| SR-27 | feed-taxonomy-imported-as-rows | invariant | The durable feed taxonomy (PRESENT + ABSENT with unlock map, expand_via/expand_priority, operator expansion ranking, per-KPI blocked flags) imports as value-model rows. | taxonomy + unlock map + flags import | "the durable knowledge imports as value-model rows — the feed taxonomy itself (the structurally-integrated PRESENT class and the un-integrated ABSENT class with its unlock map), `expand_via` / `expand_priority` semantics, the operator expansion ranking, and per-KPI blocked flags." | data-contract | core | 3/3 | transcribed | verified | |
| SR-28 | feed-operational-lag-excluded | negative | Only the operational part — per-client import lag / live coverage drift (IMPORTED "may lag per client") — stays out of the value model. | only IMPORTED lag/drift excluded | "Only the truly operational part stays OUT: per-client import lag / live coverage drift (the IMPORTED class's \"may lag per client\" status)." | data-contract | core | 3/3 | transcribed | verified | |
| SR-29 | no-connected-blocked-knowledge-dropped | invariant | The PRESENT/ABSENT split drops no connected-vs-blocked knowledge. | nothing dropped by the split | "No connected-vs-blocked knowledge is dropped by this split." | data-contract | core | 3/3 | transcribed | verified | |
| SR-30 | value-model-machine-consumable | invariant | The value-model artifact must be machine-consumable: whatever format the plan picks must parse with a stock parser (JSON/YAML/CSV class; no bespoke grammar); exact shape/format stays a plan-time call. | parses with a stock parser (JSON/YAML/CSV); no bespoke grammar | "Machine-consumable — whatever format the plan picks must parse with a stock parser (the JSON / YAML / CSV class; no bespoke grammar); the exact file shape/format remains the architect's plan-time call (skeptic SF-2 fold: gives "machine-consumable" a pass/fail floor)." | data-contract | edge | 3/3 | transcribed | verified | SF-2 fold — parse floor now the boundary; previously flagged untestable |
| SR-31 | value-model-seed-sources | outcome | The value model is seeded from the three W3-parked KG ledger entries plus the OD KB note's 24-KPI table. | two named seed sources | "Seeded from the three W3-parked KG ledger entries + the OD KB note's 24-KPI table." | data-contract | core | 3/3 | transcribed | verified | |
| SR-32 | value-briefing-sole-reader | negative | `value-briefing` is the only sanctioned reader of value content in agentic flows. | sole sanctioned reader | "A nexus-analytics plugin skill — the ONLY sanctioned reader of value content in agentic flows:" | skill | golden | 3/3 | transcribed | verified | |
| SR-33 | value-briefing-explicit-invocation | gate | `value-briefing` is invoked explicitly for an intelligence task (worth / prioritization / monetization / displacement). | explicit invocation only | "**Invoked explicitly** for an intelligence task (worth / prioritization / monetization / displacement questions)." | skill | golden | 3/3 | transcribed | verified | |
| SR-34 | default-flows-never-load-briefing | negative | The default accuracy flows (`semantic-model-query`, `data-investigation`, `answer-qa`) never load `value-briefing`. | three named flows never load it | "The default accuracy flows (`semantic-model-query`, `data-investigation`, `answer-qa`) never load it." | skill | golden | 3/3 | transcribed | verified | |
| SR-35 | briefing-loads-inputs | invariant | `value-briefing` loads the value model + cited KB sources + (where maintained) that area's open ledger entries. | value model + KB sources + optional ledger | "**Loads:** the consuming project's value model + the cited KB sources + — where the consuming project maintains a model-feedback ledger — that area's open entries." | skill | core | 3/3 | transcribed | verified | |
| SR-36 | ledger-input-optional-empty | boundary | The ledger input is optional and may legitimately be empty. | empty ledger is valid | "The ledger input is optional and may legitimately be empty" | skill | core | 3/3 | transcribed | verified | |
| SR-37 | every-number-measured-or-estimated | invariant | Every number in a briefing is labeled measured (KG-verified constructs/probe evidence) vs estimated (coefficient-derived). | binary label on every number | "a briefing in which every number is labeled **measured vs estimated** — measured = KG-verified constructs/probe evidence; estimated = coefficient-derived" | skill | golden | 3/3 | transcribed | verified | |
| SR-38 | implausibility-refused-or-flagged | gate | A dimensional-plausibility violation is refused or flagged, never emitted as a plain number. | never a bare number | "a dimensional-plausibility violation is refused or flagged, never emitted as a plain number" | skill | golden | 3/3 | transcribed | verified | |
| SR-39 | confounds-declared-with-estimates | invariant | Confounds are declared alongside any estimate they threaten. | co-located declaration | "confounds are declared alongside any estimate they threaten." | skill | core | 3/3 | transcribed | verified | |
| SR-40 | briefing-project-parameterized | invariant | `value-briefing` is project-parameterized like `mine-semantic-model`; the value-model location and inputs come from the consuming project's profile. | inputs from consuming project's profile | "**Project-parameterized** like `mine-semantic-model`: the value-model location and any project-specific inputs come from the consuming project's profile" | skill | core | 3/3 | transcribed | verified | |
| SR-41 | shipped-skill-no-kg-paths | negative | The shipped skill contains no KG-specific paths. | zero hardcoded KG paths | "the shipped skill contains no KG-specific paths." | skill | golden | 3/3 | transcribed | verified | |
| SR-42 | ledger-closure-grounded | outcome | A governed run migrates the three W3-parked ledger entries into the value model and closes them grounded — each resolution line points at value-model rows. | resolutions point at value-model rows | "A governed run migrates the three W3-parked ledger entries' content into the value model and closes them **grounded** — each resolution line points at the value-model rows." | process | golden | 3/3 | transcribed | verified | |
| SR-43 | br-c-stops-dispositioning | outcome | After closure, BR-C stops dispositioning the three entries on every subsequent run. | zero further dispositions | "BR-C stops dispositioning them on every subsequent run." | process | golden | 3/3 | transcribed | verified | |
| SR-44 | sync-extension-additive | boundary | The product repo's `scripts/sync_model.py` extension is additive (new source/dest + flag), an owner-owned change. | additive, not a replacement | "an owner-owned change in the product repo (`scripts/sync_model.py`, today a hardcoded single-bundle copy; the extension is additive — new source/dest + flag)" | sync | core | 3/3 | transcribed | verified | |
| SR-45 | sync-sequenced-after-value-model | gate | The sync extension is sequenced AFTER the value model first exists in KG. | ordering: after model exists | "sequenced AFTER the value model first exists in KG" | sync | core | 3/3 | transcribed | verified | |
| SR-46 | sync-opt-in-second-artifact | boundary | The value model is pulled as a second, opt-in sync artifact. | opt-in, not forced | "gains the value model as a second, **opt-in** artifact" | sync | core | 3/3 | transcribed | verified | |
| SR-47 | kg-web-ask-untouched | negative | KG web-ask is left untouched by this slice. | no runtime touch | "KG web-ask: untouched (negative acceptance below)." | process | golden | 3/3 | transcribed | verified | |
| SR-48 | bundle-value-free-permanent | negative | The semantic-model bundle (`seed/db/semantic-model/`) carries no value/SAP/feed fields; permanent for this slice, not deferred. | permanent, this-slice-scoped | "No value/SAP/feed fields in the semantic-model bundle (`seed/db/semantic-model/`) — this boundary is permanent for this slice, not deferred." | data-contract | golden | 3/3 | transcribed | verified | |
| SR-49 | no-kg-runtime-changes | negative | This slice makes no KG runtime changes: AskEngine, endpoints, prompts, grounding roots. | four named runtime areas | "No KG runtime changes (AskEngine, endpoints, prompts, grounding roots)." | process | golden | 3/3 | transcribed | verified | |
| SR-50 | no-probe-tooling-moved | negative | This slice does not ship or move probe tooling; W1's probe-runner/tooling generalization stays its own workstream. | probe tooling out of scope | "W1's probe-runner/tooling generalization stays its own workstream — this slice does not ship or move probe tooling." | process | core | 3/3 | transcribed | verified | |
| SR-51 | no-live-feed-coverage-tracking | negative | This slice implements no live feed-coverage tracking (ops state, not model knowledge). | no coverage tracking built | "No live feed-coverage tracking (ops state, not model knowledge)." | data-contract | core | 3/3 | transcribed | verified | |
| SR-52 | ac1-artifact-complete | outcome | A value-model artifact exists under `docs/value-model/` carrying all 24 VWH KPI rows, the S001–S007 register with both guardrail classes, and provenance for every value/weight/mapping. | 24 rows + full register + full provenance | "A value-model artifact exists under KG `docs/value-model/` carrying all 24 VWH KPI rows (pool, weight, SAP mapping where sourced), the S001–S007 coefficient register with both guardrail classes, and provenance for every value/weight/mapping." | data-contract | golden | 3/3 | transcribed | verified | |
| SR-53 | ac2-corrupt-construct-id-nonzero | gate | Corrupting one construct id makes validation exit non-zero. | must-fail demo #1 | "corrupting one construct id → non-zero exit" | data-contract | golden | 3/3 | transcribed | verified | |
| SR-54 | ac2-scalar-confidence-nonzero | gate | Introducing a scalar-confidence leaf makes validation exit non-zero. | must-fail demo #2 | "introducing a scalar-confidence leaf → non-zero exit" | data-contract | golden | 3/3 | transcribed | verified | |
| SR-55 | ac3-zero-open-dispositions | outcome | The next analytics-area run after closure reports zero still-open W3-parked dispositions. | zero-count check | "the next analytics-area run reports zero still-open W3-parked dispositions" | process | golden | 3/3 | transcribed | verified | |
| SR-56 | ac4-skill-ships-minor-changelog | outcome | `value-briefing` ships in nexus-analytics with a MINOR bump and a CHANGELOG entry. | MINOR + CHANGELOG | "The `value-briefing` skill ships in nexus-analytics (MINOR bump + CHANGELOG)" | skill | core | 3/3 | transcribed | verified | |
| SR-57 | ac4-briefing-labels-every-number | outcome | A briefing over a sample intelligence question labels every number measured vs estimated. | demonstrated, not just designed | "a briefing over a sample intelligence question labels every number measured vs estimated" | skill | golden | 3/3 | transcribed | verified | |
| SR-58 | ac4-implausibility-probe-recorded | gate | A dimensional-implausibility probe is refused/flagged, with the must-fail demo recorded. | recorded demo required | "a dimensional-implausibility probe is refused/flagged (must-fail demo recorded)." | skill | golden | 3/3 | transcribed | verified | |
| SR-59 | ac5-zero-tokens-grep | negative | A grep finds zero value/SAP/feed tokens under `seed/db/semantic-model/`. | grep-checkable, count = 0 | "zero value/SAP/feed tokens under `seed/db/semantic-model/`" | data-contract | golden | 3/3 | transcribed | verified | |
| SR-60 | ac5-ask-path-untouched | negative | KG ask-path source files are verifiably untouched. | grep/diff-checkable | "KG ask path sources untouched." | process | golden | 3/3 | transcribed | verified | |
| SR-61 | ac6-owner-run-sync-verified | outcome | The product repo pulls the value model via its sync script as an opt-in second artifact — an owner-run change made after the model exists, verified by one owner-run sync, documented there. | one recorded owner-run sync | "The product repo pulls the value model via its sync script as an opt-in second artifact — an owner-run change made in the product repo after the value model exists, verified by one owner-run sync (documented there)." | sync | core | 3/3 | transcribed | verified | |
| SR-62 | ac7-no-value-ref-in-accuracy-skills | negative | A grep over the three shipped accuracy skills finds no value-model reference. | three skills, zero refs | "The default accuracy flows contain no value-model reference — grep over the three shipped skills" | skill | golden | 3/3 | transcribed | verified | |
| SR-63 | ac7-labeling-exclusive-to-briefing | negative | Measured-vs-estimated labeling lives only in `value-briefing`; `answer-qa` gains no estimate-labeling rule and stays untouched. | answer-qa deliberately untouched | "Measured-vs-estimated labeling lives ONLY in `value-briefing` (AC-4): `answer-qa` carries no estimate-labeling rule today and is deliberately untouched in this slice" | skill | golden | 3/3 | transcribed | verified | |
| SR-64 | validator-net-new | boundary | The construct-id join check is a net-new validator capability, not an extension of the existing grammar scan (`validate-provenance.cs` validates provenance grammar only). | net-new, not an extension | "Validator home for the construct-id join check — **net-new capability**, not an extension of the existing grammar scan (`validate-provenance.cs` today validates provenance grammar only — critic L1)" | data-contract | edge | 3/3 | interpretive | ambiguous | Sits under the unresolved "validator home" open question; the net-new characterization is committed but the actionable placement (sibling tool vs extend) is deferred to plan-time — no crisp build gate. |
| SR-65 | validator-coordinate-not-preempt-w1 | negative | Work on the construct-id validator coordinates with W1's tooling-generalization lane and does not pre-empt it. | coordinate, don't pre-empt | "coordinate with W1's tooling-generalization lane, do not pre-empt it." | process | edge | 3/3 | transcribed | ambiguous | Process directive under an open question; "coordinate / do not pre-empt" has no stated pass/fail boundary (what counts as pre-empting is unspecified). |
| SR-66 | kb-sources-enrichment-not-hard-dependency | invariant | KB sources are enrichment, not a hard dependency; the value model's provenance rows carry their citations inline, so the briefing does not require a reachable KB source file to run. | inline provenance citations; KB source file not required | "KB sources are enrichment, not a hard dependency: the value model's provenance rows carry their citations inline" | skill | core | delta (SF-3 fold) | transcribed | verified | |
| SR-67 | unavailable-kb-source-disclosed | gate | Where a cited KB source file is not locally reachable, the briefing runs on the embedded provenance and discloses which sources were unavailable. | unavailable sources must be disclosed | "where a cited KB source file is not locally reachable (the product repo pulls only the value model), the briefing runs on the embedded provenance and DISCLOSES which sources were unavailable (skeptic SF-3 fold)." | skill | core | delta (SF-3 fold) | transcribed | verified | |

## Killed

- **tech-spec-w3-section-role** (miner-1 only) — "that section stays the gate/order record; this file is the W3 content authority." Describes the division of documentary authority between two spec files; a meta-statement about documents, not a testable implementation commitment of the value layer.
- **tech-spec-sentence-marked-superseded** (miner-1 only) — "The tech-spec's original W3 sentence describing that shape is marked superseded there." Documentation bookkeeping in a different file (marking a sentence superseded); a one-time meta-edit, not an invariant/gate of the value-layer build.

Also correctly NOT rules (all three miners excluded them, and the skeptic agrees): the four bullets under `## Open questions` — value-model file format, the validator home itself, the briefing output-contract shape, and whether the OD KB note is superseded — are explicitly unresolved options, not commitments. (The two directive clauses embedded in the validator open question are captured as SR-64/SR-65 and both flagged ambiguous.)

## Spec findings (dispositioned 2026-07-18)

1. **SF-1 — AC-2's golden gate rests on deferred tooling → BY-DESIGN.** SR-23/SR-53 commit fail-closed construct-id validation as a golden acceptance gate, while the validator that implements the join check is left an `## Open questions` item. Disposition: by-design — AC-2 binds the *outcome* (fail-closed validation demonstrated by must-fail cases); the open question defers only the validator *home* (sibling tool vs extend), not the gate itself.
2. **SF-2 — "Machine-consumable" (SR-30) had no crisp test → FOLDED.** The spec now floors it: the chosen format "must parse with a stock parser (the JSON / YAML / CSV class; no bespoke grammar)." Folded into SR-30 (parse floor is now the boundary; verdict stays verified).
3. **SF-3 — Briefing "cited KB sources" (SR-35) had no reachability guarantee for the first consumer → FOLDED.** The spec now makes KB sources enrichment rather than a hard dependency, with inline provenance citations and disclosure of unavailable sources. Folded into delta rules SR-66 (KB sources optional / inline citations) and SR-67 (unavailable-source disclosure).

## Citation fixes

- Miner-3 `value-model-governed-home` quoted "…`docs/value-model/`, governed — edits land only via governed runs…", eliding the clause "KG owns it;" without an ellipsis. The verbatim spec text is "…`docs/value-model/`, governed — **KG owns it;** edits land only via governed runs (ledger entry → run disposition → value model)". Corrected in SR-6 (which cites the "KG owns it" clause) and SR-7 (the edits clause).
