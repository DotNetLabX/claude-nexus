# Agent Grounding: Registry Consumers, Discoverability, and the Grounding Contract — Implementation

## Files Modified

- `plugins/nexus/agents/solo.md` — Step 1 (U1). Replaced the **Attestation drift check (pre-implementation)** paragraph (C2-conditional, dead trigger) with the pinned **Registry guardrail (pre/post-edit)** paragraph: pre-edit registry read, post-edit scoped skeptic re-verify (read-only `general-purpose` verifier, in-context fallback disclosed in "your final report"), test-update-or-M3-re-mine obligation retained, C2 forward note reworded to avoid the retired trigger phrase.
- `plugins/nexus/agents/developer.md` — Step 1 (U1). Same pinned paragraph, with "disclose it in implementation.md" per-agent variance.
- `plugins/nexus/agents/architect.md` (`## Plan Writing Rules` bullet, was "Attestation drift check (plan / done-check time)") — Step 2 (U1). Replaced with the pinned **Registry guardrail (plan / done-check time)** bullet: architect plans the registry-read + skeptic-reverify + test-update/M3-flag obligations, never executes them.
- `plugins/nexus/agents/reviewer.md` — Step 3 (U2). Two additions: item 4 in `## Before Reviewing` (registry check before reviewing a unit that has one); a **Rule-aware rider (where a registry exists)** note after the stage-gate paragraph in `## Review Dimensions`, before `### Review Axes`.
- `plugins/nexus/rules/kb-navigation.md` — Steps 4 (U3) + 5 (U4). Added `## Business-Rules Registries` section after the intro KB protocol (before `## Glossary-First Naming`) naming the registry as a navigation layer with per-row provenance/status/criticality, skip-silently-if-absent. Added `## Repo Grounding Contract` section at the end of the file (after `## Structural Navigation`) — the three thin indexes + tracked script-synced KB copy + conditional gateway clause + once-per-session missing-file surfacing, mirroring ADR-52.
- `plugins/nexus/rules/kb-maintenance.md` — Step 4 (U3). Added `## Species Boundary — Registries Are Not KB Entries` section at the end of the file: this file governs `docs/kb/` only; registries are ADR-45's own species, never hand-edited, changes flow through re-mine/skeptic re-verify.
- `plugins/nexus-cpp/skills/mine-verify-cover-cpp/SKILL.md` — Step 6 (U5). Added `## Graph extraction — clang-uml (feeds graphify-out/GRAPH_REPORT.md)` section between `## Picking a target` and `## Cost / scaling note`: clang-uml + compile_commands.json via Ninja, GraphML + JSON model output, context-radius/path-regex extraction-time god-node filtering, CodeQL licence-barred callout, Joern zero-build fallback, `docs/kb/research/cpp-code-graph-tooling.md` (omnivision repo) cited as provenance only.
- `tests/unit/kb-distill.test.mjs` — Step 7 (U6), TDD red checkpoint. Re-pointed all `ledgerPath` fixtures and the two dependent assertions (lines formerly 24, 28, 36, 55, 68, 106) plus the escaped-slash regex assertion (formerly line 72) from `docs/kb/golden/...` to `docs/business-rules/...`. Added a one-line provenance NOTE comment after the import. **Post-review fix (LOW-1, review.md ## Step 2 Finding 1):** the NOTE comment (lines 14-15) said fixtures were "re-pointed from the retired docs/business-rules/ path" — backwards, since docs/business-rules/ is the new path and docs/kb/golden/ is the retired one. Corrected to "re-pointed from the retired docs/kb/golden/ path" (comment-only change); re-ran the pinned CI form post-fix — 505 pass / 0 fail.
- `tests/unit/rules-registry.test.mjs` — Step 7 (U6), TDD red checkpoint. Updated the two header/Slice-7 comments (formerly lines 4, 129) from "OD-L5 default path docs/kb/golden/{Class}.md" to "Contract R1 default path docs/business-rules/<area>/<unit>.md".
- `harness/merge.workflow.js` — Step 7 (U6), TDD green checkpoint. `REGISTRY_PATH` default (formerly line 492) now composes `docs/business-rules/<area>/<unit>.md` via a new optional `AREA = _args.area ?? undefined`: when an area is supplied, `${SR}\docs\business-rules\${AREA}\${TARGET_CLASS}.md`; when omitted (single-area repos, Contract R1), the no-area fallback `${SR}\docs\business-rules\${TARGET_CLASS}.md`. The existing `_args.registryPath` explicit override still takes precedence, unchanged.
- `harness/lib/rules-registry.mjs` — Step 7 (U6). Updated the two comments (lines 4, 159) naming the old default path/decision label ("OD-L5 default path docs/kb/golden/{Class}.md" → "Contract R1 default path docs/business-rules/<area>/<unit>.md"). No behavior change — these are doc comments only; the pure functions never hardcoded a path.
- `plugins/nexus/commands/{solo,developer,architect,reviewer,team-lead,po,critic,learner}.md` — Step 8. Regenerated via `node scripts/gen-commands.mjs nexus` (plugin-wide, one run after all four agent edits per U1 critic observation). Verified the four touched commands (`solo`, `developer`, `architect`, `reviewer`) carry the new hook/rider text; the other four regenerate unchanged (no source agent-file edits in this slice touched them).
- `plugins/nexus/.claude-plugin/plugin.json` — Step 10. Version bumped 1.25.2 → 1.25.3 (PATCH, `bump-plugin.mjs` default) via the `release-plugin` skill.
- `plugins/nexus/CHANGELOG.md` — Step 10. Real entry authored for `[1.25.3]` replacing the tool's stub, naming the guardrail rebase (C2→C1), the review rider, discoverability, and the grounding contract (ADR-52). Phrasing guard applied: prose says "the old C2-conditional trigger", never the literal retired phrase; re-ran the Step-9 negative grep after writing — still 0 hits.
- `plugins/nexus-cpp/.claude-plugin/plugin.json` — Step 10. Version bumped 0.1.4 → 0.1.5 (PATCH, tool default).
- `plugins/nexus-cpp/CHANGELOG.md` — Step 10. Real entry authored for `[0.1.5]` naming the clang-uml/Joern graph-extraction recipe. Also fixed the documented tool quirk: `bump-plugin.mjs` inserted the new stub directly under the H1, pushing the file's descriptive paragraph ("All notable changes to the `nexus-cpp` plugin.") down past it — restored the descriptive line to directly under the H1 and placed the real `[0.1.5]` entry below it, matching the file's pre-existing structure.

## Key Decisions

- **Renamed the "OD-L5" default-path label to "Contract R1"** in the two `rules-registry.mjs` comments and the two `rules-registry.test.mjs` comments (not explicitly pinned by the plan, which only said "update the two comments naming the old default"). OD-L5 was the decision that set the *old* `docs/kb/golden/{Class}.md` default; leaving that label attached to the new path would misattribute the current default to a superseded decision. This is a same-sentence factual correction, not a scope expansion — flagged here per the "match existing pattern, deviating for cleanliness is a deviation" anti-pattern guard, even though it's a correctness fix rather than a cleanliness one.
- **`harness/merge.workflow.js` area handling** — added a new `AREA` const (`_args.area ?? undefined`) rather than threading the area through `TARGET_CLASS` or a compound arg. Matches the plan's Decision row ("optional arg + single-area fallback `docs/business-rules/<unit>.md`") and the file's existing `_args.*` convention (one const per override, e.g. `TARGET_LAYER`, `SPEC_DATE`).
- **kb-navigation.md section placement** — `## Business-Rules Registries` placed immediately after the intro KB protocol (before Glossary-First Naming) since it's a sibling navigation-layer note to the KB-index protocol above it; `## Repo Grounding Contract` placed at the end of the file (after Structural Navigation) since it's a broader repo-level contract, not a per-question navigation step. Both are free placement per the plan's "Binding surfaces" note.
- **kb-maintenance.md section placement** — `## Species Boundary` placed at the end of the file (after `## On-Demand Update`) as a standalone boundary note; free placement per the plan.
- **mine-verify-cover-cpp SKILL.md section placement** — new `## Graph extraction` section placed *after* `## Picking a target` and *before* `## Cost / scaling note` (adjacent, per the plan) rather than before Picking a target, since extraction naturally precedes and feeds the targeting decision described there.

## Skills Used

| Step | Skill(s) invoked | Notes |
|------|------------------|-------|
| 1 | None | plan-pinned text substitution, no pattern skill mapped |
| 2 | None | plan-pinned text substitution, no pattern skill mapped |
| 3 | None | plan-pinned/free-placement text addition, no pattern skill mapped |
| 4 | None | plan-pinned text addition, no pattern skill mapped |
| 5 | None | plan-pinned text addition (ADR-52 content already landed at definition time; verified present, not re-added) |
| 6 | None | self-contained recipe addition, no pattern skill mapped |
| 7 | tdd | Invoked via Skill tool before implementing. Followed red-then-green: re-pointed test fixtures/assertions first (`tests/unit/kb-distill.test.mjs`, `tests/unit/rules-registry.test.mjs`), ran the pinned CI form to confirm well-formed (505 pass / 0 fail — these are pass-through fixture tests, so "red" manifested as a correctness checkpoint on the escaped-slash regex rather than a literal failing assertion, since neither `rules-registry.mjs` nor `kb-distill.mjs` hardcodes a path), then implemented the `REGISTRY_PATH` default + optional-area-arg composition in `harness/merge.workflow.js` (green side) and re-ran the suite (505 pass / 0 fail). |
| 8 | None | mechanical regeneration step |
| 9 | None | verification-only step |
| 10 | release-plugin | Invoked via Skill tool. Dry-run classified nexus (PATCH, 1.25.2→1.25.3: agent instruction/behavior change, shipped command changed, rule injected every session) and nexus-cpp (PATCH, 0.1.4→0.1.5: skill change). Applied PATCH (the tool default) for both, per operator instruction not to escalate myself — the architect's tech-spec/plan recommendation (MINOR for both — new consumer capability / new guidance section) is the owner's call at release, recorded here for that decision. Authored real CHANGELOG entries (see Files Modified), re-ran the phrasing-guard negative grep (0 hits), validated both plugins (`claude plugin validate --strict` — both PASS), re-ran the full pinned test suite (505 pass / 0 fail). Did NOT run `gen-omni` and did NOT commit — both are owner-owned per this run's instructions. |

## Carry-Over Findings

| Title | Severity | For | Evidence | Note |
|-------|----------|-----|----------|------|
| Test count discrepancy vs plan baseline | low | reviewer | Plan's Testing Strategy cites "458 pass / 0 fail" as the pre-change baseline; this run's pinned CI form (`node --test tests/lint/*.test.mjs tests/unit/*.test.mjs`) reports **505 pass / 0 fail**, both before and after the Step-7 edits. No fail either way — the delta is additional test files/suites present in the working tree from other in-flight work, not a regression introduced by this slice. **Resolved (Step 2 review):** reviewer confirmed 505 is the correct baseline (the plan's 458 was a unit-only count); re-confirmed 505/0 again after the LOW-1 fix below. |

## Deviations from Plan

- **Step 7 comment relabeling ("OD-L5" → "Contract R1")** — see Key Decisions above. Reason: correctness (the old label misattributes the new default to a superseded decision), not scope creep; the literal path text was the pinned change and is unchanged in substance.
- **Step 7 TDD red/green shape** — the plan's "Red first" framing doesn't produce a literal failing assertion for `kb-distill.test.mjs`/`rules-registry.test.mjs`, because `distillRegistry`/`renderRegistry`/`parseRegistry` never hardcode a default path — they only echo whatever `ledgerPath`/row values a caller supplies. Documented under Skills Used (Step 7) rather than silently treating the discipline as satisfied by coincidence.
- **Step 10 — PATCH applied, not MINOR** — the plan flags the tier as "the owner's call, not an AC fact" (AC-8) and instructs recording the architect's MINOR recommendation for the owner's release-time decision rather than self-escalating. Applied the tool's PATCH default for both plugins; the MINOR recommendation is recorded in the Step 10 row of `## Skills Used` above for the owner to act on at release.
- **Did not commit, did not run `gen-omni`** — both explicitly owner-owned per this run's instructions (CLAUDE.md: "the owner owns commits and the twin sync" for this slice). The working tree is left with all Step 1–10 edits (including the version bumps and CHANGELOG entries) staged-ready but uncommitted.

## KB Impact

None — matches the tech-spec's KB Impact section (this repo's `docs/kb/` is untouched; the slice edits shipped rules *about* registries and KB navigation, not KB entries).

*Status: COMPLETE — developer, 2026-07-09*
