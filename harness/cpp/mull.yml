# mull 0.x config. The IR pass plugin (set via -fpass-plugin in CMakeLists when -DMULL=ON)
# embeds mutants at compile time; mull-runner-18 reads them from the test binary.
mutators:
  - cxx_all

# Only mutate the slice under test — never the test harness or gtest.
includePaths:
  - src/hungarian.cpp
excludePaths:
  - tests/.*
  - .*gtest.*
