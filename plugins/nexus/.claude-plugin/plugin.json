{
  "name": "nexus",
  "version": "1.2.8",
  "description": "Nexus core — a stack-agnostic multi-agent team pipeline (architect, developer, reviewer, PO, critic, learner, team-lead, solo) with always-on rules, persona commands, process skills, and a security guard.",
  "author": {
    "name": "ldumit"
  },
  "keywords": [
    "agents",
    "pipeline",
    "team",
    "workflow",
    "code-review",
    "planning"
  ],
  "userConfig": {
    "security_mode": {
      "type": "string",
      "title": "Security mode",
      "description": "Install posture: open (block catastrophic actions only — recommended) | hardened (strict, for teams/CI) | off (no guard).",
      "default": "open"
    },
    "token_audit": {
      "type": "boolean",
      "title": "Token consumption audit",
      "description": "When on, the audit-logger additionally records per-agent token usage (input/output/cache/context) to docs/audit/token-usage.jsonl on each tool call, for analysis via the consumption-report skill. Default off — zero extra work when disabled.",
      "default": false
    }
  }
}
