{
  "name": "nexus-flutter",
  "version": "0.1.1",
  "description": "Nexus Flutter (thin) — a stack extension for the `nexus` core pipeline. Ships the Dart/Flutter adapter for the mine-verify-cover method (mutation_test driving flutter test, the build_runner + HTTPS-rewrite bringup, the equivalent-mutant filter, and flutter_test + mocktail test style). Reuses nexus's agents, rules, commands, and security guard — install ALONGSIDE nexus, not instead of it.",
  "author": {
    "name": "ldumit"
  },
  "keywords": [
    "agents",
    "pipeline",
    "flutter",
    "dart",
    "mutation-testing",
    "mine-verify-cover",
    "bloc",
    "flutter-test",
    "skills",
    "extension"
  ],
  "dependencies": [
    "nexus"
  ]
}
